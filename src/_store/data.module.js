/**
 * Module for CRUD application on deck and todo data
 */

const state = {}
const getters = {}
const mutations = {}
const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
