/**
 * Global notifications for displaying errors, in-App messages and status infos
 */

const state = {}
const getters = {}
const mutations = {}
const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
